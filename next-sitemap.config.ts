/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.dnaautosource.com/',  // replace with your domain
  generateRobotsTxt: true,            // generates robots.txt too
  // optional extras:
  changefreq: 'weekly',
  priority: 0.7,
  sitemapSize: 5000,
}